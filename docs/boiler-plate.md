# Setting up any typescript project:
```
npm init -m
npm install --save express mongoose
npm install --save-dev typescript ts-node nodemon @types/node @types/express @types/jest jest ts-jest supertest @types/supertest
```

To initiate tsc
```
npx tsc --init
```

To initialise jest
```
npx ts-jest config:init
```